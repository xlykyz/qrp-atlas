# 任务 07-C：标准结果与分析产品封板

> **状态：返修中 / 等待复验**（2026-07-15）

## 目标

统一标准产品 run 结果契约，补齐发布所需的解释、复现与比较能力。

## 交付（封板口径）

- 全损合法年化：`total_return == -1` → `annual_return_pct = -100`；Calmar 在 max_dd=-100 时为 `-1`
- rolling 保留 equity=0 日期，不删除、不跨归零拼接
- benchmark/excess 进入标准 writer 产物：`benchmark.json` + summary 字段 + diagnostics（无静默替换、无缺口填充）
- MAE/MFE 通过价格窗口接入 writer（closed / open as-of）
- 完整 reproducibility 快照 + 确定性 `snapshot_hash`（`reproducibility.json` + config.reproducibility）
- 暴露产物 `exposures.json`（横截面优先；否则明确空态）
- API：rolling / costs / diagnostics / benchmark / exposures / reproducibility / compare
- 前端：workflow 可选 benchmark；分析页增加分析扩展（benchmark、rolling、暴露、复现快照）；summary 风险/费用

## 标准产物

- 既有：summary/equity/trades/skipped/config/orders/fills/snapshots
- 新增/封板：daily_returns / rolling_performance / costs / diagnostics / benchmark / exposures / reproducibility

## 边界

- 不新建策略专属结果页
- residual robustness 研究包保持独立格式
- 无归因大平台 / ML 解释 / 实时监控
