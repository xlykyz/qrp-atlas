# 任务 07-C：标准结果与分析产品封板

> **状态：验收通过 / 已合并**（2026-07-15，PR #30，merge commit `0edcac4e169ebdeef0e9428004efbb47d5f3442b`）

## 目标

统一标准产品 run 结果契约，补齐发布所需的解释、复现与比较能力。

## 最终验收关闭项

1. **excess 数学口径**
   - `daily_active_return = (1+p)/(1+b)-1`（`excess_return` 同义别名）
   - `excess_percentage_point = portfolio_cum - benchmark_cum`
   - `relative_return = (1+port_cum)/(1+bench_cum)-1`
   - benchmark 日期缺口：**不跨缺口**汇总完整区间 excess total；缺口恢复首日的 benchmark/active/excess return 保持 `None`
2. **MAE/MFE 全路径**
   - 从 fills/orders 提取实际成交资产 → 加载 OHLC 持仓窗口 → writer 填充
   - 横截面不再依赖 `request.tickers` / 缺失 `price_frame`
3. **暴露命名诚实**
   - `position_concentration` 独立字段（position_count/max_weight/sum_weight）
   - `industry` / `market_cap` 使用回测快照中的实际持仓权重 + PIT 行业成员/当日市值，并标记 `realized_portfolio_positions`
   - 不可用时明确 `industry_available` / `market_cap_available=false`，**不**把集中度写入 market_cap
4. **可执行复现**
   - reproducibility 含 resolved universe、data fingerprints、product_request 锁定快照
   - `replay_product_run` + `POST /api/backtest/runs/{run_id}/replay`
   - 标准结果包新增 `targets.json`，保存真实 execution targets
   - replay 对比策略定义、resolved universe、数据指纹、summary/equity/orders/fills/trades/execution targets

## 交付（已验收）

- 全损合法年化：`total_return == -1` → `annual_return_pct = -100`；Calmar 在 max_dd=-100 时为 `-1`
- rolling 保留 equity=0 日期，不删除、不跨归零拼接
- benchmark/excess 进入标准 writer 产物：`benchmark.json` + summary 字段 + diagnostics（无静默替换、无缺口填充）
- MAE/MFE 通过成交资产价格窗口接入全部产品路径
- reproducibility 快照 + `snapshot_hash` + **replay 重跑业务对齐**
- 暴露产物：realized `industry` / `market_cap` / `position_concentration` 分离
- API：rolling / costs / diagnostics / benchmark / exposures / reproducibility / compare / **replay**
- 前端：workflow 可选 benchmark；分析页扩展骨架保留

## 标准产物

- 既有：summary/equity/trades/skipped/config/orders/fills/snapshots
- 新增：targets（真实 execution targets）
- 封板：daily_returns / rolling_performance / costs / diagnostics / benchmark / exposures / reproducibility

## 测试

- 单元：excess 相对收益非简单差分；缺口时 full-range excess 为 None
- 端到端：`tests/backtest/test_results_product_e2e_seal.py`
  - classic / cross_sectional_momentum_long_only / event_drift_basic
  - CS 已平仓交易 MAE/MFE 非空
  - CS 真实 industry + market_cap；concentration 独立
  - replay 业务字段对齐

## 边界

- 不新建策略专属结果页
- residual robustness 研究包保持独立格式
- 无归因大平台 / ML 解释 / 实时监控
- 07-C 已封板；后续任务不得破坏该标准结果契约

## 整合状态

- 锁定验收 head：`b0baf353b6b585c8778ca2973bd66b731da804e9`
- 已通过普通 merge 合入 `main@0edcac4e169ebdeef0e9428004efbb47d5f3442b`
- PR #31 已验收并合并；07-C 契约已进入任务 08 发布验收基线
