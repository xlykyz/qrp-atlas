# 任务 07-C：标准结果与分析产品封板

> **状态：实现完成，等待 PR 验收**（2026-07-15）

## 目标

统一标准产品 run 结果契约，补齐发布所需的解释、复现与比较能力。

## 交付

- summary 增加 Sharpe / Sortino / Calmar，并保留成本与换手字段
- 标准产物新增：`daily_returns.json`、`rolling_performance.json`、`costs.json`、`diagnostics.json`
- JSON 强制无 NaN/Inf
- config 增加 `reproducibility` 锁定说明
- API 增加 orders/fills/snapshots/daily-returns/rolling/costs/diagnostics/compare
- 旧结果包缺少新文件时 costs 可从 summary 回退，缺失滚动等返回空态
- 前端 summary 展示风险与费用字段

## 边界

- 不新建策略专属结果页
- residual robustness 研究包保持独立格式
